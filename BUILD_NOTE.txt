M3-Play ready project based on InnerTune.

Important:
- Package name: com.j.m3play
- App name: M3 Play
- Root project name: M3-Play
- Firebase/Google Services full-build path is disabled by default to avoid google-services.json issues.

Recommended first build:
./gradlew assembleFossDebug

Android Studio:
Select the fossDebug variant first if needed.
